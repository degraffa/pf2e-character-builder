import React from 'react';

interface Props {}
interface State {}

export default class HeroSheetSidebar extends React.Component<Props, State> {
  render() {
    return (
      <div></div>
    );
  }
}